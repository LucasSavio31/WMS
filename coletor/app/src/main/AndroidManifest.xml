<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Acesso à rede Wi-Fi para falar com o servidor do PC -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- usesCleartextTraffic: o servidor do curso usa http (sem https) na rede local -->
    <application
        android:label="@string/app_name"
        tools:replace="android:label"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:usesCleartextTraffic="true">

        <!-- Uma tela só: a página /m do servidor dentro de um WebView.
             adjustResize: com o teclado aberto a página encolhe (o campo não some atrás dele).
             configChanges: abrir/fechar o teclado físico do MC33 não recarrega a página. -->
        <activity android:name=".MainActivity" android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
